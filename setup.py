from setuptools import find_packages, setup

setup(
    name="ai-chatbot",  # Replace with your package name
    version="0.1.0",  # Set the initial version
    description="A brief description of your package",
    author="Hadi",
    author_email="fullstackailab@gmail.com",
    url="https://github.com/full-stack-ai/ai-chatbot-backend-service",  # Update with your repo URL
    packages=find_packages(),  # Automatically finds all sub-packages
    install_requires=[],
    python_requires=">=3.10",  # Specify the Python versions your package supports
)
